{
  "name": "adas-choice-invitations",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "wrangler deploy --dry-run --outdir .wrangler/build",
    "deploy": "wrangler deploy"
  },
  "engines": { "node": ">=22" },
  "devDependencies": { "wrangler": "4.130.0" }
}
