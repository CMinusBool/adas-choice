{
  "name": "adas-choice",
  "private": true,
  "type": "module",
  "description": "Ada's choice — a hand-made apartment of co-op games, films and activities.",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc --noEmit && vite build && node scripts/assert-built-page.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.9",
    "vitest": "^5.0.0"
  }
}
