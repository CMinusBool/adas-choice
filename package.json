{
  "name": "adas-choice",
  "private": true,
  "type": "module",
  "description": "Ada's choice — a hand-made apartment of co-op games, films and activities.",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.world.json",
    "test": "vitest run && node --test scripts/check-assets.test.mjs scripts/art/codex-imagegen.test.mjs scripts/art/build-cycle.test.mjs scripts/art/make-preview.test.mjs",
    "build": "npm run typecheck && vite build && node scripts/assert-built-page.mjs && node scripts/check-assets.mjs --non-fatal"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.1.9",
    "vitest": "^5.0.0"
  }
}
